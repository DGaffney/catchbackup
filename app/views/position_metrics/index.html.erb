<h1>Listing position_metrics</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @position_metrics.each do |position_metric| %>
  <tr>
    <td><%= link_to 'Show', position_metric %></td>
    <td><%= link_to 'Edit', edit_position_metric_path(position_metric) %></td>
    <td><%= link_to 'Destroy', position_metric, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Position metric', new_position_metric_path %>
