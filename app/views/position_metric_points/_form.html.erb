<%= form_for(@position_metric_point) do |f| %>
  <% if @position_metric_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@position_metric_point.errors.count, "error") %> prohibited this position_metric_point from being saved:</h2>

      <ul>
      <% @position_metric_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
