<%= form_for(@importance_metric) do |f| %>
  <% if @importance_metric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@importance_metric.errors.count, "error") %> prohibited this importance_metric from being saved:</h2>

      <ul>
      <% @importance_metric.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
