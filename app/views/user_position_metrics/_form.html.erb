<%= form_for(@user_position_metric) do |f| %>
  <% if @user_position_metric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_position_metric.errors.count, "error") %> prohibited this user_position_metric from being saved:</h2>

      <ul>
      <% @user_position_metric.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :metric_type %><br />
    <%= f.text_field :metric_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
