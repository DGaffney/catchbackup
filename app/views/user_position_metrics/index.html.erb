<h1>Listing user_position_metrics</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Metric type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_position_metrics.each do |user_position_metric| %>
  <tr>
    <td><%= user_position_metric.name %></td>
    <td><%= user_position_metric.metric_type %></td>
    <td><%= link_to 'Show', user_position_metric %></td>
    <td><%= link_to 'Edit', edit_user_position_metric_path(user_position_metric) %></td>
    <td><%= link_to 'Destroy', user_position_metric, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User position metric', new_user_position_metric_path %>
